{
    "version": "0.2.0",
    "configurations": [
    
        {
            "request": "launch",
            "name": "Debug app",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}",
            "type": "firefox"
        }
    ]
}